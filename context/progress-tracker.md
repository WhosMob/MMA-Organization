# Progress Tracker

This file records the actual implementation state of the project so AI agents can resume development accurately across sessions.

Update this file whenever the implementation state, current phase, active feature, or important project decision changes.

The information in this file must describe the actual state of the project, not the intended or assumed state.

## Current Phase

- Homepage implementation (Hero + Events + P4P + Champions + News sections complete; Recent Results remains pending).
- Fighters listing page complete.
- Fighter Profile page complete — Hero, Information, Rankings, Championships, Upcoming Fight, Recent Fights (max 3), and Full Fight History, all using the matching redesigned fight UI.
- Event Detail page complete — Event Hero, Event Information, and Full Fight Card (Main Event / Co-Main Event / Main Card / Preliminary) reusing the extended `FightMatchupCard`.
- Events Listing page complete — page header, Featured Events timeline, and All Events section with search (name/location) + status filter (All / Upcoming / Completed) over a responsive event card grid; the nav "Events" link now resolves.
- Rankings page complete — page header, single-open Accordion (all closed by default) with the Pound-for-Pound section plus all 8 division sections, champion rows separate from #1-#15 contender rows, movement indicators, and clickable fighter rows; the nav "Rankings" link now resolves.
- News Listing page complete per `context/features-developing/16-newsPage.md` — page header, Popular News section (reusing the existing featured `NewsCarousel`), and a Latest News section with search + category filter over a responsive news card grid; the nav "News" link now resolves.
- News Detail page complete per `context/features-developing/17-newsPageSlug.md` — cinematic full-bleed Hero (category eyebrow, Barlow Condensed title, formatted publication date, "Back to News" link), a readable article body (excerpt lead + paragraphs split from `content`), and a Related News section (current article excluded, same-category first via the reusable `NewsGridCard`); all `/news/[news-slug]` links (News carousel, News grid, News Listing grid) now resolve.
- About page complete per `context/features-developing/18-AboutPage.md` — cinematic non-fullscreen Hero, Who We Are (two-column), Our Philosophy (exactly 3 cards), By the Numbers (all four stats derived from mock data), and Follow the Organization (compact social CTA reusing the brand `social-icons.tsx`); the nav "About" link now resolves.
- Contact page complete per `context/19-contactPage.md` — cinematic non-fullscreen Hero, Get in Touch (Email / Phone / Telegram cards with mailto, tel, and external Telegram links), FAQ (5-question local static array in the client FAQ component via shadcn Accordion), and Follow Us (compact social CTA reusing the brand `social-icons.tsx`); the nav "Contact" link now resolves.
- Mobile hamburger nav bug fixed — the active page's link is no longer invisible in the mobile menu (root cause: `--color-base` token collided with Tailwind's `text-base`; token renamed `bg-base` → `bg-page`).

## Current Goal

- Build out the homepage sections in order per `ui-context.md` (Hero, Events/Recent Results, P4P, Champions, News). Hero, Events, P4P, Champions, and News are complete.
- Fighters listing page (`/fighters`) is complete.
- Fighter Profile page (`/fighters/[fighter-slug]`) is complete — Hero, Information, Rankings, Championships (phase 1) plus Upcoming Fight, Recent Fights, and Full Fight History redesigned around a reusable matchup card (phase 2/3).
- Event Detail page (`/events/[event-slug]`) is complete — Hero, Information, and Full Fight Card grouped by position, reusing the matchup card with `variant="event"` + `featured` presentation.
- Events Listing page (`/events`) is complete — header, featured timeline browsing, search + status filter, and the full event grid; event cards and featured cards navigate to the Event Detail pages.
- Rankings page (`/rankings`) is complete — page header and a single-open Accordion (all closed by default) of 9 sections (Pound-for-Pound + 8 divisions) deriving from `mock-database/fighters.ts`; the nav "Rankings" link now resolves.
- News Listing page (`/news`) is complete — page header, Popular News section (reusing the homepage `NewsCarousel`), and a Latest News section with client-side search + category filtering over a responsive news card grid; the nav "News" link now resolves.
- News Detail page (`/news/[news-slug]`) is complete — cinematic full-bleed Hero, readable article body, and Related News; the nav "News" link and every existing `/news/[news-slug]` link now resolve.
- About page (`/about`) is complete — Hero, Who We Are, Our Philosophy (Competition / Respect / Integrity), By the Numbers (derived mock-data statistics), and Follow the Organization; the nav "About" link now resolves.
- Contact page (`/contact`) is complete — Hero, Get in Touch (Email / Phone / Telegram), FAQ (local static 5-question array), and Follow Us (social CTA); the nav "Contact" link now resolves.

## Completed

- Set up the design token system in `app/globals.css` (Tailwind v4 `@theme inline` mappings) covering surfaces, borders, text, accent, and state colors for both dark and light themes.
- Dark mode is the default theme; light and dark palettes are defined as CSS custom properties in `:root`/`.dark`.
- Loaded fonts via `next/font/google`: Inter (body, `--font-inter`) and Barlow Condensed (display/heading, `--font-barlow-condensed`); wired into the root layout and Tailwind tokens.
- Root layout (`app/layout.tsx`) renders the global Navbar and Footer, sets `dark` class by default, and uses `bg-base`/`text-ink` for the page shell.
- Implemented `ThemeToggle` client component (`components/layout/theme-toggle.tsx`) that toggles the `dark` class on `<html>`.
- Implemented responsive `Navbar` client component (`components/layout/navbar.tsx`): logo left, nav links (Rankings, Fighters, News, Events, About, Contact) + theme toggle right on desktop; hamburger menu + theme toggle on mobile; active link highlighting via `usePathname`.
- Extracted a shared `Logo` component (`components/layout/logo.tsx`) used by both Navbar and Footer for a consistent wordmark.
- Implemented the global `Footer` server component (`components/layout/footer.tsx`): organization logo, short description, navigation links, social links (Instagram, X, YouTube, Telegram), copyright, Privacy Policy, and Terms; responsive grid that stacks on mobile.
- Implemented stroke-style brand social icons as inline SVGs in `components/layout/social-icons.tsx`. Later replaced with **simple-icons brand icons** (same exported component names/interface kept, so the Footer's render code is unchanged): `InstagramIcon`/`XIcon`/`YouTubeIcon`/`TelegramIcon` now render the simple-icons `siInstagram`/`siX`/`siYoutube`/`siTelegram` paths via a shared `BrandIcon` wrapper (`fill="currentColor"`, `aria-hidden`, `viewBox 0 0 24 24`). This is the only shared consumer of `social-icons.tsx` (the About Follow section switched to simple-icons directly), so the Footer's layout, spacing, styling, URLs, and accessibility are all untouched.
- Implemented the Homepage Hero section (`components/home/hero.tsx`, Server Component): introduces the website / organization identity — "The Ultimate Fighting League" headline in Barlow Condensed, welcome label, org mission copy, and a centered row of equal-width nav-link CTAs (Fighters, Rankings, News, Events, About, Contact) styled as outline buttons; a downward chevron scroll indicator (soft `scroll-bounce` keyframe) anchors to the Events section below; cinematic Next `<Image>` background with dark gradient overlay, token-driven and responsive. It contains no event data.
- Implemented the Homepage Events section (`components/home/events-section.tsx`, Server Component) directly below the Hero as a single "Events" section: section heading + one event carousel. The former featured "Next Event" block and separate "Past Events" heading were removed per user direction.
- Implemented the Events carousel (`components/home/events-carousel.tsx`, Client Component): one chronological timeline of all events where the center card shows the highlighted/current event with faded side previews of the previous (left) and next (right) events peeking behind it; prev/next arrow buttons, keyboard navigation (arrow keys), mobile swipe, and the current event receives the strongest visual emphasis. Previews render on `md:`+ and hide on small screens.
- Event cards use a cinematic presentation: full-bleed image with a dark gradient scrim, an "Upcoming" accent pill (or ghost date pill for past events), a MapPin venue · location line, hover image zoom, and the centered card is emphasized with a subtle red glow ring + deep shadow.
- Created the `mock-database/` development data layer per the architecture Development Data Strategy: `mock-database/events.ts` exposes a typed `MockEvent` + `upcomingEvent`, `futureEvents` (added one 2027 event "MMA — Warpath"), `pastEvents`, and a date-sorted `allEvents` timeline mirroring the target Prisma Event model. `mock-database/fighters.ts` contains 128 fighters with embedded ranking data, champion status, movement indicators, and an `imageUrl` field (currently `""` for all fighters).
- Added server-side data access helper `lib/events.ts` (`getEvents()`) — the future swap point for Prisma queries; UI consumes mock data, holds no mock definitions. `getUpcomingEvent()`/`getPastEvents()` were removed (replaced by `getEvents()`).
- Configured `images.remotePatterns` for the mock event artwork host in `next.config.ts` to support Next `<Image>` with the external URL.
- Implemented the Homepage P4P section (`components/home/p4p-section.tsx`, Server Component): "Pound for Pound" heading in Barlow Condensed, "Top 5 fighters in the world" subtitle, ranking list with position numbers, fighter names, nicknames, weight classes, records, and ranking movement indicators (UP/DOWN/STABLE/NEW); clickable rows link to fighter profiles; "View Full Rankings →" link to `/rankings`.
- `lib/rankings.ts` derives P4P and division rankings from `mock-database/fighters.ts` (removed standalone `mock-database/rankings.ts`); exports `getP4PRankings()`, `getDivisionRankings()`, `getChampion()`, `getAllChampions()`.
- Homepage (`app/page.tsx`) renders `<Hero />`, `<EventsSection />`, `<P4PSection />`, and `<ChampionsSection />` in the correct order.
- Verified with `npm run lint` (passes), `npm run build` (passes), and a live render check (hero CTAs + scroll arrow, Events heading, carousel arrows, and the centered "Night of Champions" / previewed "Warpath" events all render).

- Implemented the Homepage Champions section (`components/home/champions-section.tsx`, Server Component): "Champions" heading in Barlow Condensed, 8 champions in a 4-column grid (all viewports), cards scale down responsively on mobile (smaller padding, text, gaps); each card shows weight class, champion name, nickname, and image; cards link to fighter profiles; derived from `mock-database/fighters.ts` using `getAllChampions()` helper; ordered by canonical weight-class order (Heavyweight → Flyweight); no hardcoded champion names.
- Added `imageUrl?: string | null` to `MockFighter` interface, later made required (`imageUrl: string`) with every fighter set to `""` awaiting real image URLs.
- Added `unsplash.com` to `images.remotePatterns` in `next.config.ts` (event images reference this domain).
- Champion name text color uses inline `style={{ color: "var(--text-primary)" }}` instead of Tailwind `text-ink` utility — the Tailwind token was not resolving correctly in this component context.
- Implemented the Homepage News section (`components/home/news-section.tsx`, Server Component): "News" heading in Barlow Condensed with "Popular &amp; Featured" eyebrow, placed after the Champions section in `app/page.tsx`; renders the popular news carousel and an empty-state message when no popular news exists.
- Created `mock-database/news.ts` as the single source of truth for news: `MockNews` interface (`id`, `title`, `slug`, `excerpt`, `content`, `imageUrl`, `publishedAt`, `isPopular`) plus 8 fictional articles coherent with the org (Derek Lewis superfight at MMA 40, Night of Champions title fights, Marcus Rivera showcase, Jamal Washington, flyweight trilogy, prospects feature, Volkanovski interview, MMA — Warpath at MSG). `isPopular` is deliberately independent of recency: the newest article is NOT popular while two older articles ARE.
- Added `lib/news.ts` (`getPopularNews()`) — filters `news` by `isPopular`; the future swap point for Prisma queries. UI holds no news content.
- Implemented the News carousel (`components/home/news-carousel.tsx`, Client Component): single-slide track carousel with `translateX(-current * 100%)` and 500ms easing; exactly 5-second autoplay interval (single `setInterval` cleared on cleanup, reset on slide change/manual nav); autoplay pauses on mouse enter/focus and resumes on leave/blur; reduced-motion preference (via `useSyncExternalStore` on `matchMedia`) disables autoplay and the slide transition; prev/next arrow `Button`s, dot indicators, keyboard arrow navigation on the focusable `role="region"`, and mobile swipe (>40px delta). Slides link to `/news/[slug]` from the mock data.
- Implemented the News card (`components/home/news-card.tsx`, presentational): image with dark gradient scrim and backdrop-blurred publication-date pill (lucide `Calendar`), Barlow Condensed title, two-line-clamped excerpt; token-driven surfaces/text; hover image zoom + `border-line`/shadow lift; `group-hover:text-accent-primary` title.
- Verified with `npm run lint` (passes; only pre-existing `no-img-element` warning in champions-section.tsx), `npm run build` (passes, static prerender of `/`), and a live render check (News heading, `id="news"` anchor, carousel region, and a `/news/[slug]` link all present in the served HTML).
- Adjusted Homepage Hero section height: changed `min-h-160` to `min-h-[calc(100vh-4rem)]` so the hero fills exactly the viewport minus the navbar, keeping the scroll arrow visible without scrolling on both desktop and mobile. Reduced inner content padding (`pt-10 lg:pt-6`, `gap-4`) for tighter vertical spacing.

- Implemented the Fighters listing page (`/fighters`): Server Component page (`app/fighters/page.tsx`) with page header (title + description) and a Client Component `FightersBrowser` for interactive search/filter.
- Created `lib/fighters.ts` server-side data access helper: exports `getAllFighters()` and `WEIGHT_CLASSES` constant (canonical 8 divisions in project order). Future swap point for Prisma queries.
- Created `components/fighters/fighters-browser.tsx` (Client Component): search input (case-insensitive against name + nickname), weight class dropdown filter (All + 8 divisions), both filters compose with AND logic, empty state with "Clear filters" reset, fighter count display, responsive grid (4-col mobile, 6-col xl, 8-col 2xl). Select padding adjusted to `pl-4 pr-8` for proper text alignment on Windows native dropdowns.
- Created `components/fighters/fighter-card.tsx` (presentational): fighter image with dark gradient overlay, name + nickname in the image overlay, weight class + record row below, champion badge pill (top-right), P4P rank badge when ranked, division rank badge when ranked, clickable Link to `/fighters/[fighter-slug]`, hover scale + border/shadow lift consistent with homepage card patterns. All typography, badges, and padding fully scaled for 4-col mobile layout (champion badge `text-[7px]`, name `text-[10px]`, division badge shows only `#N` on mobile). Nickname line always renders to keep names vertically aligned.
- Verified with `npm run lint` (passes; only pre-existing `no-img-element` warnings), `npm run build` (passes, static prerender of `/fighters`).
- Extended the mock fighter database (`mock-database/fighters.ts`) per `context/features-developing/09-revise-the-fighters-datbase.md`: added `nationality`, `dateOfBirth` (`YYYY-MM-DD`), `height` (`F'IN / cm`), `reach` (`F'IN / cm`), and `championships` (array of `{ title }`) to every fighter. Added the `MockFighterChampionship` interface. No `age` field, no championship `status` field, no fight/event data added (deferred to a future `mock-database/fights.ts`), and existing IDs/slugs, records, rankings, `isChampion`, and movement fields were preserved unchanged. Fictional but internally coherent biographical data (nationalities, DOBs, physicals) was authored per fighter; current champions hold their division title, and a small set of established veterans (Whittaker, Ferguson, dos Anjos, Poirier, Oliveira, Holloway, Yan, Aldo, Edgar, Figueiredo) carry former-champion title entries. Verified with `npx tsc --noEmit`, `npm run lint`, `npm run build`, and a field-presence/lint script (all pass; only pre-existing duplicate P4P #1/#2 data and image warnings remain).
- Standardized the fighter `imageUrl` field in `mock-database/fighters.ts`: changed `MockFighter.imageUrl` from `string | null | undefined` to a required `imageUrl: string`, added `imageUrl: ""` to every fighter, and removed the previous placeholder paths (`/gaziev.jpg`, `/orolbai.jpg`). All 128 fighters now carry `imageUrl: ""` awaiting real image URLs. No other fighter field was touched. Verified with `npx tsc --noEmit` (passes).

- Implemented the Fighter Profile page (`/fighters/[fighter-slug]`) Phase 1 per `context/features-developing/10-fighterProfile.md`. Server Component page (`app/fighters/[fighter-slug]/page.tsx`) with `generateStaticParams()` (all 128 fighter slugs), `generateMetadata()` (title `${name} — MMA Organization`, description built from fighter data, "Fighter Not Found" fallback), and `notFound()` from `next/navigation` for unknown slugs. Renders exactly four sections — Fighter Profile Hero, Fighter Information, Rankings, Championships. Fight History and Recent Fights are intentionally NOT built yet (deferred to a future `mock-database/fights.ts` per the spec).
  - Added `lib/calculate-age.ts` → `calculateAge(dateOfBirth)` (age computed dynamically; correctly handles birthdays not yet passed).
  - Added `getFighterBySlug(slug)` to `lib/fighters.ts`; data source remains `mock-database/fighters.ts` (no new data source created).
  - `FighterProfileHero` (`components/fighters/fighter-profile-hero.tsx`): Barlow Condensed name (5xl→7xl), weight-class eyebrow, CHAMPION pill when `isChampion`, nickname, record, P4P/division rank pills, `aspect-[3/4]` image panel with initials fallback when `imageUrl` is empty and no broken `<img>`; uses `<img>` like existing fighter cards (same pre-existing lint warning).
  - `FighterInformationSection` (`components/fighters/fighter-information-section.tsx`): semantic `<dl>` grid (2-col mobile → 5-col lg) with styled stat cards — Nationality (Globe), Date of Birth (CalendarDays, raw string), Age (Cake, via `calculateAge`), Height (Ruler), Reach (ArrowLeftRight); height/reach displayed exactly as stored.
  - `FighterRankingsSection` (`components/fighters/fighter-rankings-section.tsx`): two cards — P4P (accent) and division (neutral). Champions (`rankings.division === null` + `isChampion`) display "Champion" and are never treated as Division Rank #1; unranked slots show "Not Ranked".
  - `FighterChampionshipsSection` (`components/fighters/fighter-championships-section.tsx`): returns `null` when `championships` is empty (no empty/meaningless section); otherwise a Trophy-anchored title list with a "Current Champion" sub-label when `isChampion`.
  - All components are Server Components, token-driven only, follow the existing section eyebrow + Barlow Condensed h2 patterns; no new libraries added.
  - Verified with `npx tsc --noEmit` (passes), `npm run lint` (passes; only pre-existing `no-img-element` warnings including the new hero), `npm run build` (passes; 133 static pages — `/`, `/_not-found`, `/fighters`, and 130 fighter profile pages including the `[fighter-slug]` dynamic route), and live `next start` checks: valid slug returns 200 with correct name/nickname/record/P4P #13/division "Champion"/age/rankings/championships; invalid slug returns 404; empty `imageUrl` renders initials placeholder; non-champion ranked fighter shows division `#1` and no empty championships section; champion without P4P rank shows P4P "Not Ranked" and division "Champion"; age boundary cases correct (Volkanovski `1988-09-29` → 37 before birthday, Rivera `1992-05-09` → 34 after birthday; reference date 2026-09-06).
- Applied the `text-ink` workaround (same as champions-section) to all Fighter Profile `text-ink` elements: replaced the Tailwind class with inline `style={{ color: "var(--text-primary)" }}` on the hero `h1`/record `span`, info section `dd`/`h2`, rankings section division number/`h2`, and championships section title/`h2`. This fixes the theme-color resolution issue where `text-ink` produced near-invisible text in both dark and light modes on these elements. Verified with `npx tsc --noEmit`, `npm run lint`, `npm run build` (all pass; 133 static pages).
- Made the hero division ranking pill display the weight class on all viewports: removed `hidden sm:inline` from `fighter-profile-hero.tsx` so mobile shows `#1 LIGHT HEAVYWEIGHT` like desktop (previously mobile showed only `#1`). Verified with `npx tsc --noEmit`, `npm run lint`, `npm run build` (all pass).

- Created `mock-database/fights.ts` per `context/features-developing/11-matchesMockdata.md` — the source of truth for fight data during the mock phase. Exports strongly typed `FightStatus`, `FightOutcome`, `FightMethod`, `FightCardPosition`, `FightResult`, `Fight` interfaces plus a `fights` array of 60 fights (38 completed, 22 upcoming) across all 6 existing events (`evt-001` Night of Champions, `evt-002` MMA 40, `evt-003` MMA 39, `evt-004` MMA 38, `evt-005` MMA 37, `evt-006` Warpath). Every fight references existing event/fighter IDs only, has exactly one card position (one MAIN_EVENT + one CO_MAIN_EVENT per event), a weight class from the canonical 8, and `result` (or `null` for upcoming). Results cover WIN/DRAW/NO_CONTEST and all six methods (KO, TKO, SUBMISSION, UNANIMOUS/SPLIT/MAJORITY_DECISION); the dataset includes one majority-decision DRAW (nam vs zhumagulov, MMA 39) and one NO_CONTEST (curtis vs kopylov, MMA 40). The MMA 40 main event is an intentional champ-vs-champ superfight (HW champion shamil-gaziev def. LHW champion jamal-washington, KO rd2) mirroring the `mock-database/news.ts` "Lewis vs Washington" headline mapped onto the existing fighter DB; the moreno/figueiredo trilogy (MMA 38 reclaim + Warpath finale) mirrors news nws-005, NOC main/co-main (adesanya vs volkov, sterling vs dvalishvili) match nws-002, Rivera's non-title NOC showcase vs tsarukyan matches nws-003, and Warpath (FLW+WW+LHW titles, "three divisions") matches nws-008. Verified with `npx tsc --noEmit`, `npm run lint` (only pre-existing warnings), `npm run build` (133 static pages, unchanged), and a dedicated data-integrity script covering all 17 spec checks (event/fighter refs, no self-fights, unique ids, ≤1 ME/CoME per event, UPCOMING→null result, COMPLETED→valid result, WIN winner/loser ∈ fight, DRAW/NC→null ids, date coherence, valid enums, no `any`). No UI, Prisma, fighters.ts, or events.ts changes.

- Implemented Fighter Profile Phase 2 per `context/features-developing/12-fighthistory.md`: connected the Fighter Profile page to `mock-database/fights.ts` and added **Recent Fights** + **Full Fight History** sections.
  - Created `lib/fights.ts`: exports `FighterFight` (fight + resolved opponent `MockFighter`, event `MockEvent`, weight class, card position, raw result, and derived fighter outcome) and `getFighterFights(fighterId)` — selects fights where `fighter1Id === fighterId || fighter2Id === fighterId` (no duplicates), resolves the opponent dynamically as the OTHER fighter, resolves the event via `eventId` through `getEvents()`, derives the fighter's outcome from `fight.result` (WIN/LOSS via `winnerId`, DRAW, NC) or `null` for upcoming, and sorts by date descending (string `YYYY-MM-DD` `localeCompare`). Fights whose opponent/event cannot be resolved are skipped defensively. No `any`, no weakening of `mock-database/fights.ts` types, all narrowing via `status`/`result` checks.
  - Added `getEventById(id)` to `lib/events.ts`.
  - Created reusable presentational `FighterFightRow` (`components/fighters/fighter-fight-row.tsx`, Server Component) shared by both sections: result pill (WIN = accent-dim, LOSS = neutral, DRAW = warning, NC = muted, UPCOMING = outlined accent), method + `Rd N · time` for completed fights, opponent name linked to `/fighters/[fighter-slug]`, weight class · card position line, event name linked to `/events/[event-slug]`, and a `Intl.DateTimeFormat` date (no third-party date library). Uses the inline `style={{ color: "var(--text-primary)" }}` mitigation for primary text (known `text-ink` issue); all other text uses `text-ink-secondary`/`text-ink-muted`/`text-ink-faint`/accent tokens.
  - Created `FighterRecentFightsSection` (`components/fighters/fighter-recent-fights-section.tsx`): completed fights only, date desc, capped at 5, returns `null` when the fighter has no completed fights; upcoming fights are NOT mixed in.
  - Created `FighterFightHistorySection` (`components/fighters/fighter-fight-history-section.tsx`): all fights (completed + upcoming) date desc, returns `null` when the fighter has no fights at all.
  - Wired both sections into `app/fighters/[fighter-slug]/page.tsx` after Championships (order: Hero → Information → Rankings → Championships → Recent Fights → Fight History). Existing four sections untouched. No `"use client"`, no API routes, no new dependencies.
  - Verified with `npx tsc --noEmit` (passes), `npm run lint` (passes; only the 3 pre-existing `no-img-element` warnings), `npm run build` (passes, 133 static pages — SSG count unchanged), and live `next start` HTML checks: Rivera shows Recent Fights (WIN TKO Rd2·1:32 vs Oliveira @ MMA 39, WIN TKO Rd3·2:11 vs Ferguson @ MMA 37) then Fight History ordered upcoming → completed desc (UPCOMING vs Tsarukyan @ Night of Champions Nov 14 2026 first); tyson-nam shows DRAW vs zhumagulov; chris-curtis shows NC vs kopylov; figueiredo shows UPCOMING vs Moreno (Warpath) with the completed LOSS further down — opponent identity is always the OTHER fighter; no-fight fighter (blagoy-ivanov) returns 200 with neither fight section rendered; invalid slug still 404.

- Implemented the Event Detail page (`/events/[event-slug]`) per `context/features-developing/13-eventPages.md`.
  - Added `getEventBySlug(slug)` to `lib/events.ts` (slug lookup on `mock-database/events.ts`; the existing `id`-based `getEventById` is unchanged).
  - Added `getEventFights(eventId)` to `lib/fights.ts`: returns all fights where `fight.eventId === eventId` as `FighterFight[]` (both fighters + event resolved), preserving the source `fights.ts` array order (deterministic; the array is already grouped by card position in intended display order). No new fight/event data structure introduced.
  - Extended the reusable `FightMatchupCard` (`components/fighters/fight-matchup-card.tsx`) with two optional props instead of duplicating the card:
    - `variant?: "fighter" | "event"` (default `"fighter"`) — in `"event"` mode the center result is neutral: the big label shows the global winner's name (accent) / "Draw" (warning) / "No Contest" (muted) instead of a fighter-perspective WIN/LOSS; the winner ring + "Winner" tag remain winner-based (already neutral); the footer shows the bout weight class instead of the self-referential event-name link.
    - `featured?: boolean` — larger fighter imagery, larger VS/headline, wider card, accent ring for Main/Co-Main presentation.
    - Fighter Profile rendering is 100% unchanged (default `"fighter"` variant).
  - Created `EventHero` (`components/events/event-hero.tsx`, Server Component): cinematic full-bleed event artwork with dark scrims, status pill (accent "Upcoming" / ghost "Event Results"), Barlow Condensed event name, description, and a CalendarDays + MapPin metadata row.
  - Created `EventInformation` (`components/events/event-information.tsx`, Server Component): semantic `<dl>` with four stat cards — Date, Location, Venue, Status — using only fields that exist in `MockEvent`.
  - Created `FightCardSection` (`components/events/fight-card-section.tsx`, Server Component): groups fights by `cardPosition` (Main Event featured, Co-Main Event featured, Main Card, Preliminary) with per-group headings; empty group sections are not rendered; an event with zero fights shows a "Fight Card Coming Soon" empty state instead of crashing.
  - Created the page (`app/events/[event-slug]/page.tsx`): Server Component with `generateStaticParams()` (all 6 event slugs), `generateMetadata()` (title `${event.name} — MMA Organization`, description from event data, "Event Not Found" fallback), and `notFound()` for unknown slugs. Section order: Event Hero → Event Information → Full Fight Card. Fully static (SSG), no `"use client"`, no API routes, no new dependencies, mock-data layer untouched.
  - Removed the pre-existing broken partial Event-page attempt files (`components/events/event-information-section.tsx`, `components/events/event-fight-card-section.tsx`) that referenced a non-existent `EventFight` type and unsupported `emphasis`/`highlighted` props.
  - Verified with `npx tsc --noEmit` (passes), `npm run lint` (passes; only the pre-existing `no-img-element` warning set), `npm run build` (passes, 139 static pages — 133 previous + 6 event pages), and live `next start` checks: `/events/night-of-champions` 200 with Main Event/Co-Main/Main Card/Preliminary headings, both fighter names, "VS" + Upcoming pill; `/events/mma-37-all-in` 200 with winner name + "Winner" tag + TKO result (no perspective WIN/LOSS text, correct); `/events/mma-39-homecoming` shows Rivera vs Oliveira main event and DRAW fighters (tyson-nam / zhumagulov); unknown slug returns 404; existing `/fighters/marcus-rivera` profile (Hero, Information, Championships, and Fight History sections) unchanged.

- Redesigned the Fighter Profile fight UI and added a dedicated Upcoming Fight section (UI/UX refinement only; fight data model and mock data untouched).
  - Extended `FighterFight` in `lib/fights.ts` with `selfId`, `fighter1`, `fighter2` (resolved `MockFighter` instances; the original `fighter1Id`/`fighter2Id` order is preserved so Fighter 1 always renders on the left); `opponent` remains resolved as the OTHER fighter. No mock data, types, or architecture changes.
  - Created reusable `FightMatchupCard` (`components/fighters/fight-matchup-card.tsx`, Server Component): the single fight card shared by Upcoming / Recent / Full History. Desktop face-off grid `[1fr_auto_1fr]` with fighter portrait panels on both sides and a center VS/result column; same 3-column layout scales down intentionally on mobile (images smaller, no overflow). Fighter panels (whole panel is a `<Link>` to `/fighters/[fighter-slug]`, matching the fighter-card pattern) show image with gradient scrim + initials fallback when `imageUrl === ""` (no broken images), Barlow Condensed name, nickname when present, record, and weight class. The globally winning side gets an accent ring + "Winner" tag; champion fighters show a "Champion" tag. Center column: UPCOMING → big "VS"; COMPLETED → the selected fighter's outcome (WIN accent / LOSS neutral / DRAW warning / NC muted) derived via `fight.outcome`, plus formatted method and `R{n} · time`. Card position labels are polished ("Main Event" etc.); event name links to `/events/[event-slug]` and forms the footer with an `Intl.DateTimeFormat` date. Upcoming variant gets a more prominent treatment (accent border + filled "Upcoming" pill + `bg-elevated`); no WIN/LOSS/DRAW/NC/method/round/time is ever rendered for upcoming fights.
  - Wired `FighterUpcomingFightsSection` (`components/fighters/fighter-upcoming-fights-section.tsx`) between Championships and Recent Fights: shows every upcoming fight sorted date ASC (nearest first), renders nothing (returns `null`) when the fighter has zero upcoming fights — no empty state.
  - Rewrote `FighterRecentFightsSection`: completed fights only, date DESC, exactly the 3 most recent (`RECENT_FIGHTS_LIMIT = 3`; shows all when fewer exist), returns `null` at 0 completed fights. Upcoming fights are never mixed in.
  - Rewrote `FighterFightHistorySection`: all fights (completed + upcoming) date DESC with clear visual distinction between the two status variants; returns `null` only when the fighter has no fights at all.
  - Replaced the old `FighterFightRow` (removed `components/fighters/fighter-fight-row.tsx`); the page now renders Hero → Information → Rankings → Championships → Upcoming Fight → Recent Fights → Full Fight History.
  - All theme colors use tokens only (accent/ink-secondary/ink-muted/ink-faint/warning/subtle/elevated/line tokens); the primary-text workaround (`style={{ color: "var(--text-primary)" }}`) is reused for fighter names; focus-visible rings added on fighter/event links; subtle motion only (image scale + border/ring transitions, no autoplay). No `"use client"`, no API routes, no new dependencies.
  - Verified with `npx tsc --noEmit` (passes), `npm run lint` (passes; only the known `no-img-element` warning pattern, now 4 total incl. the new card), `npm run build` (passes, 133 static pages), and live `next start` HTML checks across: Volkanovski (0 completed / 1 upcoming → Recent absent, Upcoming present), blagoy-ivanov (no fights → no fight sections), tyson-nam (DRAW), chris-curtis (NC), Figueiredo (LOSS recent + upcoming history; upcoming card verified free of any result/method text), Gaziev (WIN + real image `/gaziev.jpg` + winner tag), Jamal Washington (fighter1 Gaziev renders LEFT with "Winner" tag, center LOSS, real Gaziev image on opponent page), Max Holloway (no upcoming section), Jose Aldo (1 completed + 1 upcoming), Marcus Rivera (2 completed shown, 1 upcoming); invalid slug still 404. Note: the current 60-fight dataset contains no fighter with ≥3 completed fights (max is 2 — Rivera, Holloway, Chimaev), so the 3-cap slice was verified by code inspection and by the ≤3-per-fighter data check; the `slice(0, 3)` logic is exercised whenever such a fighter is added.

- Implemented the Events Listing page (`/events`) per `context/features-developing/14-event-page.md`.
  - Added `EventListItem` (a `MockEvent` extended with `mainEvent: { fighter1; fighter2 } | null` and `fightCount`) and `getEventListItems()` to `lib/fights.ts`. The main event is derived live from `mock-database/fights.ts` (`eventId` + `cardPosition === "MAIN_EVENT"`, fighter names resolved through `mock-database/fighters.ts`), `fightCount` counts the event's fights, and event status comes from the existing `MockEvent.status` — no duplicated event/fight data and no new event status field.
  - Created the Events page (`app/events/page.tsx`, Server Component): static page header (eyebrow + "Events" title + supporting description), a Featured Events section (rendered only when events exist), and an All Events section. `export const metadata` (`title`/`description`) follows the fighters-page convention. No `"use client"`, no API routes, no search params (search/filter is client-side state matching the Fighters browser pattern).
  - Created `FeaturedEventsTimeline` (`components/events/featured-events-timeline.tsx`, Client Component): timeline-style browsing — previous/active/next cards with faded side previews on `md:`+, prev/next arrow `Button`s, keyboard navigation (arrow keys on the focusable `role="region"`), mobile swipe (>40px delta), an "N / count" label plus dot indicators for the timeline position, and NO autoplay. The active card gets the strongest emphasis (accent ring + deep shadow) and shows the status pill, date, venue · location, the main-event matchup ("A vs B") when available, and the fight count. Every featured card links to `/events/[event-slug]`.
  - Created `EventCard` (`components/events/event-card.tsx`, presentational, consumed by the client browser): image with dark gradient scrim + status pill (accent "Upcoming" / ghost "Completed"), event name, date, venue · location, "Main Event: A vs B" when available, and a "View Event →" affordance; the whole card is a `<Link>` to `/events/[event-slug]` with a visible focus ring; image-less events fall back to a token gradient + `Swords` icon instead of a broken image.
  - Created `EventsBrowser` (`components/events/events-browser.tsx`, Client Component): search input (matches event name and location) plus a shadcn `Select` status filter with exactly All / Upcoming / Completed; search and filter compose with AND logic; result count; responsive grid 1-col mobile / 2-col tablet / 3-col desktop; polished empty states for an empty dataset ("No events yet") and for a search/filter miss ("No events found" + clear button).
  - Verified with `npx tsc --noEmit` (passes), `npm run lint` (passes; only the 4 pre-existing `no-img-element` warnings), `npm run build` (passes, 140 static pages — 139 previous + `/events`). Live `next start` HTML checks were NOT run (starting the server was declined during this session), so runtime rendering of the search/filter/timeline interactions was not live-verified.

- Mobile UI refinement of the reusable `FightMatchupCard` (`components/fighters/fight-matchup-card.tsx`). Desktop layout is 100% untouched (all desktop values verified identical to the pre-change classes; all changes live in base/mobile classes with `sm:`+ restoring the original); fight data model, props, and variants unchanged. Win/Loss/Draw/NC and Upcoming behavior verified live on `/events/mma-37-all-in`, `/events/night-of-champions`, `/fighters/jamal-washington`, and `/fighters/deiveson-figueiredo`.
  - **Root-cause fix for the winner-name/mobile-layout problem:** the center column used a fixed `auto` grid track and oversized mobile outcome text (`text-4xl` fighter variant / `text-6xl` VS). The grid now uses `grid-cols-[minmax(0,1fr)_auto_minmax(0,1fr)]` (columns can actually shrink — the old `1fr` behaved as `minmax(auto,1fr)` and overflowed with long names) and the center column gained `min-w-0`. Mobile outcome text dropped to `text-xl` (fighter) / `text-3xl` (featured) and VS to `text-3xl`/`text-4xl`; the winner name in the center (event variant) stays confined by the shrinkable center track. All fighter names are single-line `truncate` on mobile with a uniform `text-sm`, so the result state can never enlarge a name, resize an image, change a column width, or shift the VS/result position. Fighter images get explicit mobile caps (`max-w-[7rem]` standard / `max-w-[8rem]` featured) while desktop sizes are unchanged.
  - **Winner vs loser hierarchy (mobile-only):** winner — accent name + `font-extrabold`, `Winner` accent badge, accent ring (mobile `ring-1`/desktop `ring-2` preserved), accent-tinted initials fallback gradient, accent-tinted initials glyph; loser — whole panel `opacity-60`, secondary/`ink-faint` name + record, muted image (`brightness/saturate 0.85`, restored to 100 on desktop), softened border. Desktop winner/loser treatment is byte-identical to the original.
  - **Mobile badges/typography:** Winner/Upcoming/Champion badges tightened on mobile (smaller padding, `text-[8px]`, `rounded`), desktop `rounded-md px-2.5 py-1 text-[10px]` preserved. Card-position label, method, and `R{n} · time` scaled down on mobile and restored at `sm:`/`lg:`. Card padding `p-3` mobile vs `sm:p-6 lg:p-8` preserved; footer also scaled for mobile.
  - Verified with `npx tsc --noEmit` (passes), `npm run lint` (passes; only the 4 pre-existing `no-img-element` warnings), `npm run build` (passes; 140 static pages, unchanged), and live `next start` HTML checks confirming the emitted mobile classes and that the arbitrary `text-[var(--text-primary)]`/`text-[var(--text-faint)]` responsive color utilities compile in the production CSS.

- Second pass on the reusable `FightMatchupCard` mobile layout: fix the root cause where the winner still appeared to sit in the center and image sizes/column widths changed with which fighter won. Mobile-only again; every desktop (`sm:`/`lg:`) class string is preserved unchanged (verified via `git diff` against the previous version and live HTML).
  - **Second-pass refinement per user feedback:** after the structural fix, the mobile (`sm:hidden`) center label of completed cards now shows a permanent **"VS"** styled exactly like the upcoming VS (`text-ink-faint`, `font-heading` bold caps; `text-4xl` featured / `text-3xl` standard — same string as the upcoming span) instead of any outcome text, on both variants (event pages and fighter profiles). The old `getEventMobileResultText`/`getMobileResultText` helpers and the `mobileResultText` const were removed (now dead code). Desktop is untouched: the `hidden sm:block` label still renders the winner name / Draw / No Contest (event) or WIN/LOSS/DRAW/NC (fighter) exactly as before, and the mobile below-grid method/`R·time` row is unchanged.
  - **Root cause (all three):** (1) the event-variant center label rendered the winning fighter's full name via `getEventResultText()` → winner's name sat physically in the center on Event Detail pages; (2) the center column is an `auto` grid track sized by its content, so a long winner name widened it and squeezed the `minmax(0,1fr)` fighter columns — and since fighter images were fluid `w-full`, the images physically shrank with the column; (3) there were no fixed mobile image dimensions or fixed center width.
  - **Structural fix (no overrides, no shrinking fonts to hide the problem):** the center column is now a fixed-width element on mobile (`w-20 shrink-0`, 80px) and back to content-width on desktop (`sm:w-auto sm:shrink`); fighter images are fixed-size on mobile (`w-24 shrink-0`, 96px — fits the 100px columns available at a 320px viewport) and fluid again on desktop (`sm:w-full` + the original `sm:max-w-[14rem] lg:max-w-[16rem]` featured / `sm:max-w-[10rem] lg:max-w-[12rem]` standard). The result label is split into two spans: a mobile (`sm:hidden`) compact neutral label — event variant shows "Winner"/"Draw"/"NC" (**never a fighter name**; new `getEventMobileResultText` / `getMobileResultText` helpers), fighter variant keeps its WIN/LOSS/DRAW/NC — and the original desktop label as `hidden sm:block` (event variant still shows the global winner's name there, unchanged). Method + `R{n} · time` moved out of the center for mobile: desktop keeps them in the center (`hidden sm:block`), and a new `sm:hidden` result row renders below the grid on mobile. Result outcome is now the only thing the center column ever contains, so no combination of winner (fighter1 or fighter2), name length, card position, method, or outcome can change image sizes or column widths.
  - Verified with `npx tsc --noEmit` (passes), `npm run lint` (passes; only the 4 pre-existing `no-img-element` warnings), `npm run build` (passes; 140 static pages, unchanged), and live `next start` HTML checks on `/events/mma-39-homecoming` (Case A: fighter1 wins — mobile center shows `Winner`, desktop span shows the winner name + TKO + round, fixed `w-24` images, `w-20` center, below-grid result row) and on `/events/mma-38-ground-zero` with a **temporarily flipped** `moreno-vs-figueiredo-2` winnerId (Case B: fighter2 wins — figueiredo stays in the RIGHT column with the winner ring/badge/accent, moreno stays LEFT with the mobile loser treatment, center still shows neutral `Winner`; data immediately reverted and build re-run so no mock data changed, `git status` clean for data files). Note: the 60-fight dataset contains no actual fighter2-wins result, so Case B was exercised via the flip.

- Implemented the Rankings page (`/rankings`) per `context/features-developing/15-createRankingsPage.md`.
  - Added the shadcn/ui `Accordion` foundation component (`components/ui/accordion.tsx`) via the shadcn CLI (Base UI React accordion primitive, `cn`-based, matching the existing `button`/`select` foundation components). No other foundation components were modified.
  - Created `lib/rankings-data.ts` (`getRankingsPageData()`): server-side composition that fetches the P4P top-15 via `getP4PRankings(15)` and, for each of the 8 `WEIGHT_CLASSES`, the division champion via `getChampion()` plus the #1–#15 contenders via `getDivisionRankings(wc, 15)`. Data is derived live from `mock-database/fighters.ts` (no new `rankings.ts` data source, no `"Not Ranked"` strings, no duplicated ranking data) per spec section 16; each division returns `champion: MockFighter | null`.
  - Created `RankingsAccordion` (`components/rankings/rankings-accordion.tsx`, Client Component): a single-open shadcn `Accordion` (default `type` is single-open → only one section open at a time, all closed on initial load, closing the open section is supported). Renders exactly 9 sections = 1 Pound-for-Pound + 8 divisions. Section header is a compact clickable `AccordionTrigger` (Barlow Condensed uppercase title, ChevronUp/Down indicator, `hover:bg-surface/50` + visible focus ring) with a subtle accent treatment on P4P (accent left border + accent dot) while staying in the same Accordion system. Each section renders a `RankingList` of `RankingRow`s.
  - `RankingRow` (`components/rankings/rankings-accordion.tsx`): compact row = rank number (Barlow Condensed, `text-ink-faint`, hover → accent) + circular fighter photo (or polished `bg-subtle` initials fallback when `imageUrl` is empty, matching the existing fallback treatment) + fighter name (truncated, `min-w-0 flex-1` so long names never push the movement off-card) + movement indicator on the far right. Movement renders `▲N` (success), `▼N` (error), `NEW` (accent), or `—` (muted) via semantic tokens (color is not the only differentiator — the arrows/dash/`NEW` text are always present), reusing the same movement data model as `p4p-section.tsx`. The whole row is a `<Link>` to `/fighters/[fighter-slug]` with hover feedback.
  - `ChampionRow` (`components/rankings/rankings-accordion.tsx`): visually distinct but restrained — `CHAMP` badge pill (accent), accent-tinted border + `bg-accent-primary/5` background, slightly larger photo with accent ring — and renders separately ABOVE the #1–#15 contender rows, never occupying a numbered rank. The champion is never assigned `rankings.division = 1` (data uses `isChampion`).
  - Server Component page (`app/rankings/page.tsx`): static header (eyebrow-style `h1` "Rankings" + supporting description), fetches via `getRankingsPageData()` and passes the data into the Client `RankingsAccordion`. Mock data is not accessed from the client. `export const metadata` follows the fighters-page convention. `ImageUrl` handling uses `<img>` like existing fighter cards (matching the same pre-existing `no-img-element` warning pattern — 6 total warnings now).
  - Verified with `npx tsc --noEmit` (passes), `npm run lint` (passes; only the pre-existing `no-img-element` warning set, now 6 total incl. the 2 new ranking rows), `npm run build` (passes; 141 static pages — 140 previous + `/rankings`). Behavior verified by inspection against the data layer (P4P #1–#15, each division champion + #1–#15, Champion separated from #1, single-open accordion, all closed initially, movement indicators, row links, empty-image fallback, responsive long-name handling). Live `next start` HTML/interaction checks were not run during this session.
- Rankings refinements after initial delivery:
  - Fixed fighter-name color inversion (black in dark / white in light): both `RankingRow` and `ChampionRow` name `<span>`s hit the known `text-ink`-does-not-resolve bug, so their `text-ink` class was replaced with the established inline `style={{ color: "var(--text-primary)" }}` workaround (same as champions-section and fighter-profile components). The `group-hover:text-accent-primary` class remains but is inert like in champions-section.
  - Removed the underline that appeared under the fighter names: the shadcn `AccordionContent` base string applies `[&_a]:underline [&_a]:underline-offset-3` to every link inside the content. Since `[&_a]:underline` (specificity 0,1,1) beats a `no-underline` class (0,1,0), the fix uses inline `style={{ textDecoration: "none" }}` on both the `RankingRow` and `ChampionRow` `<Link>`s (inline beats any selector). The shared `accordion.tsx` foundation component was intentionally left untouched.
  - Fixed P4P fighter photos not rendering: `MockRanking` in `lib/rankings.ts` had no `imageUrl` field, so `fighterToRanking()` dropped the image and both P4P and division contender rows hardcoded `imageUrl: ""` (initials fallback). Divisions looked correct only because their `ChampionRow` uses the full `MockFighter` (real image) while P4P has no champion row. Fix: added `imageUrl: string` to `MockRanking`, populated it from `fighter.imageUrl`, and passed `ranking.imageUrl` into both P4P and division `RankingRow`s. Re-verified `npx tsc --noEmit`, `npm run lint`, and `npm run build` all pass (141 static pages unchanged).

- Implemented the News Listing page (`/news`) per `context/features-developing/16-newsPage.md`. The News Detail page (`/news/[news-slug]`) is intentionally NOT built (handled separately after visual approval, per spec section 15).
  - Extended `MockNews` in `mock-database/news.ts` with a `category: NewsCategory` field (new `NewsCategory` union type `"FIGHTERS" | "EVENTS" | "ORGANIZATION" | "RANKINGS"`); assigned a category to every one of the 8 articles. No new data source was created (the existing `mock-database/news.ts` remains the single source of truth).
  - Extended `lib/news.ts`: added `getAllNews()` (returns the full collection sorted date descending) and re-exported `MockNews`/`NewsCategory`; the existing `getPopularNews()` was preserved. UI holds no news content.
  - Created the Server Component page (`app/news/page.tsx`): static page header (eyebrow "Editorial" + "News" Barlow Condensed title + supporting subtitle), a Popular News section that renders the existing homepage `NewsCarousel` (reused — not duplicated) when popular news exists, and a Latest News section rendering the `NewsBrowser`. `export const metadata` follows the pages convention. The `/news` link in the Navbar now resolves.
  - Created `NewsBrowser` (`components/news/news-browser.tsx`, Client Component): Lucide `Search` input (label sr-only, `placeholder="Search news..."`) matching the event/fighter-browser input treatment, plus a horizontal pill category filter (ALL / Fighters / Events / Organization / Rankings) — active category is a filled accent pill, inactive pills are clean/understated; horizontal scroll on mobile (`-mx-4 overflow-x-auto`) with no page overflow. Search (title + excerpt, case-insensitive) and category filter compose with AND logic. Result count, a responsive grid (3-col desktop / 2-col tablet / 1-col mobile), and two polished empty states ("No stories yet" for an empty dataset, "No stories found" + clear-reset for a search/filter miss). Search/filter are client-side state — the page stays static and the mock-data layer is untouched.
  - Created `NewsGridCard` (`components/news/news-grid-card.tsx`, presentational): whole-card `<Link>` to `/news/[news-slug]` with visible focus ring, consistent `aspect-video` image with dark gradient scrim, category pill (top-left), Barlow Condensed title (inline `style={{ color: "var(--text-primary)" }}` workaround for the known `text-ink` issue), date, two-line-clamped excerpt; hover border/shadow lift (no excessive scaling/glow). Design tokens only, no hardcoded colors.
  - Verified with `npx tsc --noEmit` (passes), `npm run lint` (passes; only the 6 pre-existing `no-img-element` warnings), `npm run build` (passes; 142 static pages — 141 previous + `/news`). Interaction behaviors (search, each category, combined search+category, carousel autoplay/pause-on-hover/swipe/keyboard/reduced-motion, empty state, no horizontal overflow) were implemented per the existing patterns and verified by inspection; live `next start` HTML checks were not run during this session.

- Implemented the News Detail page (`/news/[news-slug]`) per `context/features-developing/17-newsPageSlug.md`.
  - Added `getNewsBySlug(slug)` to `lib/news.ts` (slug lookup on `mock-database/news.ts`; the existing `getAllNews()`/`getPopularNews()` are unchanged). No new news data source was created; `imageUrl`/`publishedAt`/`category` come from the existing `MockNews` objects.
  - Created the Server Component page (`app/news/[news-slug]/page.tsx`) with `generateStaticParams()` (all 8 article slugs), `generateMetadata()` (title `${article.title} — MMA Organization`, description from the excerpt, "Article Not Found — MMA Organization" fallback), and `notFound()` for unknown slugs. Fully static (SSG); no `"use client"`, no API routes, no new dependencies.
  - Created `NewsHero` (`components/news/news-hero.tsx`, Server Component): the news image is the page's Hero visual — full-bleed `Next <Image>` (`fill` + `priority`) with a dark cinematic gradient overlay, content anchored to the lower portion; category pill (white text over `black/40` backdrop-blur, accent dot), Barlow Condensed title (`text-4xl` mobile → `text-6xl` desktop, `break-words` protection), and a human-readable publication date (`Intl.DateTimeFormat`, e.g. "September 5, 2026"). Responsive heights (`min-h-[60vh]`) that reduce naturally on mobile while keeping the image present; no raw ISO timestamps. `imageUrl === ""` renders a token-gradient placeholder panel instead of a broken image.
  - Created `ArticleBody` (`components/news/article-body.tsx`, Server Component): comfortable `max-w-3xl` reading column; the excerpt renders as a visually distinct lead (accent left-border, larger weight, `text-ink-secondary`) directly beneath the hero; a human-readable date line with `dateTime` attribute; the `content` field is split on newlines into trimmed non-empty paragraphs rendered as separate `<p>` elements with a spacing-y `space-y-6`, divided from the lead by a subtle top border. Inter body text on token colors (`text-ink-secondary`), no excessive bold/red.
  - Created `RelatedNews` (`components/news/related-news.tsx`, Server Component): "Keep Reading" eyebrow + "Related News" heading, rendering up to 3 related articles via the existing reusable `NewsGridCard` (from the News Listing browser — not duplicated). Deterministic selection: same category first (date desc), then other categories (date desc); the current article is always excluded. Returns `null` when nothing else exists.
  - Shared section-heading pattern (eyebrow with accent dot + Barlow Condensed `h2`) follows the News Listing page; the heading-title inline `style={{ color: "var(--text-primary)" }}` workaround is reused for the article body's headings.
  - Verified with `npx tsc --noEmit` (passes), `npm run lint` (passes; only the 6 pre-existing `no-img-element` warnings), `npm run build` (passes; 150 static pages — 142 previous + 8 news detail pages), and live `next start` checks: valid slug (`/news/lewis-washington-superfight-delivers`) returns 200 with title + category "EVENTS" + formatted date + "Back to News" + hero image (`alt` present) + Related News where the current article is excluded (links: two-title-fights, marcus-rivera, jamal-washington — all EVENTS category first); `/news/marcus-rivera-night-of-champions` renders 4 separate body paragraphs + excerpt lead + correct `<title>`/description metadata; invalid slug `/news/not-a-real-article` returns 404.

- Implemented the About page (`/about`) per `context/features-developing/18-AboutPage.md`.
  - Created `lib/about-stats.ts` (`getAboutStats()`): derives all statistics purely from the existing mock-data layer — Divisions from `WEIGHT_CLASSES.length`, Events from `getEvents().length`, Fighters from `getAllFighters().length`, and Completed Fights by counting `mock-database/fights.ts` fights with `status === "COMPLETED"` (38). No hardcoded numbers, no new data source, no duplicated statistics data.
  - Created the Server Component page (`app/about/page.tsx`): static `export const metadata` (title "About — MMA Organization", description) following the pages convention; section order Hero → Who We Are → Our Philosophy → By the Numbers → Follow the Organization (→ the site-wide Footer, which renders globally from `app/layout.tsx` — the existing Footer is reused, no second Footer created). Fully static (SSG), no `"use client"`, no API routes, no new dependencies.
  - `AboutHero` (`components/about/about-hero.tsx`): cinematic, deliberately non-fullscreen hero (`min-h-[55vh]` mobile → `min-h-[65vh]` sm+) visually consistent with the homepage Hero — full-bleed external MMA image from `images.unsplash.com` (already whitelisted in `next.config.ts`), `opacity-50` + `bg-linear-to-r from-base via-base/80 to-base/40` overlay, accent eyebrow, Barlow Condensed "The Organization Behind the Fights" headline, and a short strong statement. Primary headline uses the established inline `style={{ color: "var(--text-primary)" }}` workaround. No invented founding story/founder/date/location.
  - `WhoWeAre` (`components/about/who-we-are.tsx`): desktop two-column layout (large `aspect-[4/3]` MMA visual on the left with gradient scrim; heading + three concise paragraphs on the right) that stacks on mobile. Copy explains the organization is built around high-level MMA competition, fighters as the core, events bringing competition together, and rankings/championships providing structure. No fake history or real-world claims.
  - `PhilosophySection` (`components/about/philosophy-section.tsx`): exactly three cards — Competition (`Swords`), Respect (`Handshake`), Integrity (`ShieldCheck`) — all Lucide icons verified present in lucide-react 1.41.0. Uses the existing card language (`bg-surface`, `border-line`, `rounded-lg`, hover `bg-subtle` + border lift); short, MMA-specific descriptions; no animations (no reduced-motion machinery required). The card description paragraphs initially used `text-ink-secondary` but hit the known token-resolution bug in this card context (black in dark / white in light) — fixed with the established inline `style={{ color: "var(--text-secondary)" }}` mitigation (post-review refinement).
  - `ByTheNumbers` (`components/about/by-the-numbers.tsx`): semantic `<dl>` hairline grid (`gap-px` over `bg-line-subtle`, `overflow-hidden rounded-xl border`, `grid-cols-2` mobile → `lg:grid-cols-4`) with large Barlow Condensed numbers (primary text token) and small uppercase labels (muted); consumes `getAboutStats()` so the section stays correct automatically as mock data changes.
  - `FollowSection` (`components/about/follow-section.tsx`): compact pre-footer CTA — "Follow the Organization" heading, supporting text ("Stay connected with the latest events, fighters, rankings and news."), and four accessible social buttons (Instagram, X, YouTube, Telegram) using **proper simple-icons brand icons** (user-directed refinement: replace the generic/hand-drawn social icons with brand icons — Lucide stays for UI/functional icons only). `simple-icons@^16.30.0` was added to dependencies (it was not previously installed; no other icon library was installed). The About Follow section renders each icon via its `path` from the simple-icons data object (`siInstagram`, `siX`, `siYoutube`, `siTelegram`); all other styling is unchanged — same URLs as the Footer, `target="_blank"` + `rel="noopener noreferrer"`, meaningful per-link `aria-label`s (the brand `<svg>` is `aria-hidden`), visible `focus-visible` rings, and a subtle hover accent. The global `components/layout/social-icons.tsx` component is UNTOUCHED (the Footer still uses it). The supporting paragraph text uses the inline `style={{ color: "var(--text-secondary)" }}` mitigation for the known token bug. Re-verified after the swap with `npx tsc --noEmit` (passes), `npm run lint` (passes; only the 6 known `no-img-element` warnings — no new warnings), `npm run build` (passes; 151 static pages unchanged).
  - Verified with `npx tsc --noEmit` (passes), `npm run lint` (passes; only the 6 known `no-img-element` warnings — no new warnings), `npm run build` (passes; 151 static pages — 150 previous + `/about`, static prerender). Live `next start` HTML checks were not run during this session.

- Implemented the Contact page (`/contact`) per `context/19-contactPage.md`.
  - Created the Server Component page (`app/contact/page.tsx`): static `export const metadata` (title "Contact — MMA Organization", description) following the pages convention; section order Hero → Get in Touch → FAQ → Follow Us (→ the site-wide Footer, which renders globally from `app/layout.tsx` — the existing Footer is reused, verified exactly one `<footer>` in the served HTML). Fully static (SSG, `/contact` prerendered), no API routes, no new dependencies.
  - `ContactHero` (`components/contact/contact-hero.tsx`): cinematic but deliberately shorter than the homepage Hero (`min-h-[45vh]` mobile → `min-h-[55vh]` sm+, vs About `55vh`/`65vh`) and visually consistent with the About hero — same full-bleed `<Image>` + `bg-linear-to-r from-base via-base/80 to-base/40` overlay + accent eyebrow + Barlow Condensed headline ("Contact the Organization") with the established inline `style={{ color: "var(--text-primary)" }}` workaround. Supporting paragraph covers general inquiries, partnerships, and media questions. No invented address/office location; first-letter casing and spacing follow the About hero exactly.
  - `GetInTouch` (`components/contact/get-in-touch.tsx`): the primary section — "Reach Out" eyebrow + "Get in Touch" Barlow Condensed h2, one supporting line, and three contact cards (`bg-surface`, `border-line`, `rounded-lg`, hover `bg-subtle` + border lift): **Email** (Lucide `Mail`, `mailto:contact@mmaorganization.com`), **Phone** (Lucide `Phone`, `tel:+15550123456`), **Telegram** (simple-icons brand icon via the shared `components/layout/social-icons.tsx` `TelegramIcon`, external link to `https://t.me/MMAOrganization`). Grid is 1-col mobile → 3-col tablet/desktop (`sm:grid-cols-3`). No contact form, no map, no working hours.
  - `FaqSection` (`components/contact/faq-section.tsx`, the page's only Client Component): the FAQ content is a simple local static `faqs` array (5 questions) inside the component — no `mock-database/faq.ts`, no DB, no API, no data fetching. Reuses the existing shadcn/base-ui `Accordion` foundation (uncontrolled, single-open, all collapsed by default via `defaultValue` omitted), styled to match the project (Barlow Condensed uppercase question text with the inline `style={{ color: "var(--text-primary)" }}` workaround, `rounded-lg border border-line bg-surface` container, `pt-0 pb-2.5` panel). Questions are specifically about contacting/navigating the organization (contact channels, upcoming events, rankings + fight results, media/partnership inquiries, following updates) — no generic MMA-education questions. The page remains a Server Component; only this section is client (`"use client"`), matching the `RankingsAccordion` project pattern. Refinement: the FAQ answer `<p>` uses inline `style={{ color: "var(--text-secondary)" }}` instead of `text-ink-secondary` — the answer text hit the same `text-ink`-family token-resolution bug inside the Accordion content (white in light mode / black in dark mode), so the established inline-style mitigation is applied (verified in the built HTML and live render).
  - `FollowUsSection` (`components/contact/follow-us-section.tsx`): compact pre-footer CTA mirroring the About Follow the Organization section (same `border-t border-line-subtle bg-elevated` treatment, centered column, `size-11` rounded social buttons with `size-5` icons), with heading "Follow Us" and the exact supporting text "Stay connected with the latest events, fighters, rankings and news." Instagram / YouTube / X / Telegram reuse the shared simple-icons `social-icons.tsx` components (no new icons, no new icon library) with the same URLs as the Footer and About (`https://instagram.com`, `https://x.com`, `https://youtube.com`, `https://telegram.org`), `target="_blank"` + `rel="noopener noreferrer"`, meaningful per-link `aria-label`s, and visible `focus-visible` rings.
  - Contact data convention: the project defines no organization email/phone/Telegram anywhere, so clearly-fictional placeholder values consistent with the org's mock-data phase were used (brand-level, not personal): `contact@mmaorganization.com`, `+1 (555) 012-3456`, `https://t.me/MMAOrganization` / `@MMAOrganization`. No `mock-database/contact.ts` was created (the spec allows simple static content on this page).
  - Verified with `npx tsc --noEmit` (passes), `npm run lint` (passes; only the 6 pre-existing `no-img-element` warnings — no new warnings from this feature), `npm run build` (passes; **152 static pages** — 151 previous + `/contact`, prerendered static content), and live `next start` HTTP checks on `/contact`: status 200 with all four sections present (hero heading, "Get in Touch" + all three channel cards with `mailto:`/`tel:`/external Telegram links, "FAQ" + all 5 questions incl. the long media/partnership one, "Follow Us" + 4 social `aria-label`ed buttons), exactly one `<footer>` (the global one), and the correct `<title>` metadata.

- Implemented the global Not Found page (`app/not-found.tsx`) — a single shared 404 used for every unmatched route and every existing `notFound()` call (fighters/events/news detail pages). Server Component only; renders inside the existing root layout so the global Navbar and Footer wrap it automatically (verified exactly one `<header>`/`<footer>` — no duplicated shell).
  - Composition follows the established brand language: dominant Barlow Condensed "404" display number (`text-7xl` → `lg:text-9xl`) with a restrained two-token gradient (`bg-linear-to-b from-ink to-ink-muted` + `bg-clip-text text-transparent`, decorative `aria-hidden`), an uppercase "Page Not Found" `h1` (inline `style={{ color: "var(--text-primary)" }}` mitigation), a thin `bg-accent-primary` hairline accent, a supporting sentence using only the token palette (inline `var(--text-secondary)` mitigation), and a single **Back to Home** `<Link href="/">` styled via the shared `buttonVariants({ variant: "default", size: "lg" })` (solid accent CTA, overridden to `h-12 rounded-md px-8 text-sm font-semibold uppercase tracking-wide` to match the hero CTA family). Centered with `flex-1` inside `<main>`; responsive paddings; no images, no animations, no client state, no API routes, no new dependencies, no metadata export (not-found files do not support `metadata`; the root-layout title applies).
  - Verified with `npx tsc --noEmit` (passes), `npm run lint` (passes; only the 6 pre-existing `no-img-element` warnings — no new warnings), `npm run build` (passes; 152 static pages, `/ _not-found` now built from the custom page), and live `next start -p 3100` HTTP checks: unmatched route `/some/deep/route` → 404 with fully server-rendered layout (header/footer/main once, `404` glyph, "Page Not Found", hairline, copy, `href="/"` CTA); invalid slugs `/fighters/not-a-real-fighter`, `/events/not-a-real-event`, `/news/not-a-real-article` → 404 via my global not-found UI (custom classes + copy present in the response, Next default 404 absent; runtime dynamic-slug 404s are delivered as streamed `__next_error__` shells whose flight payload contains the not-found markup plus the client ThemeProvider/Navbar refs); valid `/` and `/fighters/marcus-rivera` → 200. Light/dark safe because the page uses only semantic tokens inside the existing theme system.

- Fighter Profile Rankings refinement (`components/fighters/fighter-rankings-section.tsx`): the Pound for Pound card is now omitted entirely when the fighter has no P4P rank (previously it rendered a muted "Not Ranked" card). When P4P is present the two cards render in the existing 2-column grid (`sm:grid-cols-2`); when absent only the division card renders, constrained to `max-w-md` (single-column, no empty grid cell). The division card behavior is unchanged (rank number / "Champion" / "Not Ranked"), and the `RankingCard` "Not Ranked" branch remains for the division card. Verified with `npx tsc --noEmit` (passes), `npm run lint` (passes; only the 6 pre-existing warnings), `npm run build` (passes; 152 static pages unchanged), and live `next start` checks: `/fighters/marcus-thompson` (p4p null, division #2) → no "Pound for Pound"/"Not Ranked" cards, only the `#2` division card in a `max-w-md` container; `/fighters/shamil-gaziev` (p4p #1) → both cards in the 2-column grid.
- Added a **Weight** stat card to the Fighter Information section (`components/fighters/fighter-information-section.tsx`), placed between Age and Height (order: Nationality → Date of Birth → Age → Weight → Height → Reach), using the reusable `InfoItem` with the Lucide `Weight` icon and the grid widened `lg:grid-cols-5` → `lg:grid-cols-6`. The value is the fighter's division weight limit via a new canonical lookup in `lib/fighters.ts` (`WEIGHT_CLASS_LIMITS` record keyed by `WeightClass` + `getWeightLimit(weightClass)` with a `"—"` fallback), displayed in the file's combined imperial/metric style (e.g. Heavyweight `"265 lbs / 120 kg"`). No mock-database, Prisma, or data-model changes; per the user's decision the limit is a division attribute (not a per-fighter field), so all fighters in a division show the same value. `architecture-context.md` updated to note weight classes carry a canonical weight limit surfaced on fighter profiles. Verified with `npx tsc --noEmit` (passes), `npm run lint` (passes; only the 6 pre-existing warnings), `npm run build` (passes; 152 static pages unchanged), and a live `next start` check confirming card order (Nationality → Date of Birth → Age → Weight → Height → Reach), `lg:grid-cols-6`, and correct division values on `/fighters/shamil-gaziev` (`265 lbs / 120 kg`) and `/fighters/marcus-rivera` (`155 lbs / 70 kg`).
- Fixed the News Detail `Back to News` placement (`components/news/news-hero.tsx`): the link was inside the bottom-anchored (`justify-end`) hero content stack so it rendered at the bottom of the image above the category pill. The hero container is now `justify-between` with `gap-10` — the link is pinned top-left (`py-2` touch target, `text-white/80 hover:text-white` for contrast over the image) and the title/category/date block stays bottom-anchored (`pb-8 sm:pb-10 lg:pb-12`). Single-file change, Server Component preserved. Verified with `npx tsc --noEmit` (passes), `npm run lint` (passes; only the 6 pre-existing warnings), `npm run build` (passes; 152 static pages unchanged).
- Fixed Champions card text overflow on mobile (`components/home/champions-section.tsx`, grid untouched per user direction — stays `grid-cols-4`): the division label, fighter name, and nickname are now single-line `truncate` with `whitespace-nowrap overflow-hidden` (`LIGHT HEA…`, `ALEXANDER VOL…`, `"The Assassin…"`), the text container and card got `min-w-0`/`w-full` so ellipsis engages, and full values remain available via `title` attributes. Desktop unaffected (truncate is a no-op on wide cards). Verified with `npx tsc --noEmit` (passes), `npm run lint` (passes; only the 6 pre-existing warnings), `npm run build` (passes; 152 static pages unchanged).

- Fixed invisible active link in the mobile hamburger menu (reported: after navigating to a page, that page's nav link appears missing/invisible in the menu). **Root cause:** the `--color-base` design token in `app/globals.css` (`@theme inline`) collided with Tailwind v4's built-in `--text-base` font-size token. Tailwind resolved the ambiguous `text-base` utility as a **color** (`color: var(--bg-base)`) instead of a font-size, dropping the font-size rule entirely (`--text-base` emitted 0 times). Because that color rule sorts after `.text-accent-primary` in the generated CSS, any element pairing `text-base` with `text-accent-primary` (the mobile nav links — desktop uses `text-sm`, so it never collided) rendered the active link's text `var(--bg-base)` (dark: `#080808` on `#080808`, light: `#0a0a0a` on `#fafafa`) → effectively invisible. The same collision silently disabled `1rem` font-size for all 22 `text-base` usages across the codebase (they only looked right because the browser default is 16px and `sm:text-lg` covered desktop). **Fix (token layer, root cause):** renamed the Tailwind color mapping `--color-base` → `--color-page` (utility `bg-base` → `bg-page`) in `app/globals.css` + all 7 component usages + the `body @apply` (`components/layout/navbar.tsx`, `components/layout/footer.tsx`, `components/home/hero.tsx`, `components/news/article-body.tsx`, `components/news/related-news.tsx`, `components/about/by-the-numbers.tsx`). The CSS variable `--bg-base` and the `page background` token name in `ui-context.md` are unchanged. After the change `.text-base{font-size:var(--text-base)}` re-emits, the `text-base`/`--bg-base` color rule disappears, and the mobile active link shows brand red. No other `text-elevated`/`text-surface`/`text-subtle`/`text-line` token collides with a Tailwind `--text-*` key, so this is the only such collision. Active styling remains red-text-only (per user direction — no background pill). Verified with `npx tsc --noEmit` (passes), `npm run lint` (passes; only the 6 pre-existing `no-img-element` warnings), `npm run build` (passes; 152 static pages), headless-Chrome mobile-viewport (390px) checks of `/news`, `/rankings`, `/fighters` in light + dark: active link computed color `rgb(225, 6, 0)` (brand red) on the page background, inactive links `rgb(112, 112, 112)`; dev + prod CSS confirmed `.text-base{font-size:var(--text-base)}`, `.bg-page{background-color:var(--bg-base)}`, `.bg-page\/80` opacity variant, `--text-base: 1rem` restored, and zero `.bg-base` utility rules.

- Fighter card (`components/fighters/fighter-card.tsx`) mobile-only refinements: the record now sits on its own line below the weight class (record and weight class stack vertically via `flex-col` on mobile, side-by-side at `sm:`+); name/nickname, weight class/record, and rankings badges are all centered on mobile (`text-center sm:text-left`, `items-center`, `justify-center sm:justify-start`); desktop layout is untouched.

## In Progress

- None yet.

## Next Up

- Implement the Recent Results homepage section (horizontal carousel per `ui-context.md`); `mock-database/fights.ts` provides the completed-fight data it needs (each derived result → WIN/LOSS/DRAW/NC). This is the only remaining homepage section.

## Mock Database Refactor

### Refactored: Fighter Ranking Architecture

**What changed:**
- Created `mock-database/fighters.ts` with all fighter data including embedded ranking information
- Removed `mock-database/rankings.ts` (ranking data now derived from fighters)
- Updated `lib/rankings.ts` to derive rankings from fighters data
- Updated `components/home/p4p-section.tsx` to use new types from `lib/rankings.ts`

**New fighter data structure:**
```typescript
interface MockFighter {
  id: string;
  name: string;
  nickname: string | null;
  record: string;
  weightClass: string;
  nationality: string;
  dateOfBirth: string;
  height: string;
  reach: string;
  championships: MockFighterChampionship[];
  imageUrl: string;
  rankings: {
    p4p: number | null;    // null = not ranked P4P
    division: number | null; // null = not ranked in division
  };
  isChampion: boolean;
  movement: {
    p4p: RankingMovement | null;
    division: RankingMovement | null;
  };
  p4pMovementChange: number;
  divisionMovementChange: number;
}
```

**Ranking data coverage:**
- P4P rankings: #1 through #15 (15 fighters)
- 8 weight classes: Heavyweight, Light Heavyweight, Middleweight, Welterweight, Lightweight, Featherweight, Bantamweight, Flyweight
- Each division has 1 champion + 15 ranked contenders (#1 through #15)
- Total: 128 fighters (8 champions + 120 contenders)
- Champions are displayed separately from contender rankings
- Movement indicators (UP/DOWN/STABLE/NEW) included for each ranking type

**Data access functions:**
- `getP4PRankings(limit)` — returns P4P rankings sorted by position
- `getDivisionRankings(weightClass, limit)` — returns division rankings sorted by position
- `getChampion(weightClass)` — returns the champion for a specific division
- `getAllChampions()` — returns all champions

**Verification:**
- No duplicate P4P ranking positions
- No duplicate division ranking positions within the same weight class
- Every division has one current champion
- Every division has contenders ranked #1 through #15
- Homepage P4P still displays only the Top 5 (derived from full P4P ranking)
- No unused rankings.ts file remains

## Open Questions

- None.

## Known Issues

- Reserve Tailwind's `text-*` font-size namespace for typography: do not name any color token `--color-base` (or any other `--text-*` key such as `xs`/`sm`/`lg`/`xl`/`2xl`…). A color token with those names makes Tailwind resolve `text-<name>` as a color instead of a font-size and, depending on cascade order, can override other text-color utilities. This was the root cause of the invisible-mobile-link bug (documented in the Completed section): `--color-base` turned `text-base` into `color: var(--bg-base)` and clobbered `text-accent-primary`.
- Tailwind `text-ink` utility does not resolve correctly for text color inside certain component contexts (Champions card `<Link>`, Fighter Profile `dd`/`h1`/`h2` elements, Event Information `dd` values). Workaround: use inline `style={{ color: "var(--text-primary)" }}` where theme-aware text color is needed in those contexts. Other sections (P4P, navbar, etc.) use `text-ink` without issue.
- Hero heading (`text-6xl`) and CTA buttons (`w-36`) overflow on small mobile screens (< 400px). Needs responsive sizing for mobile viewports.

## Architecture Decisions

- Design tokens are exposed as Tailwind utility colors (`bg-page`, `bg-surface`, `bg-elevated`, `bg-subtle`, `border-line`, `border-line-subtle`, `text-ink`, `text-ink-secondary`, `text-ink-muted`, `text-ink-faint`, `text-accent-primary`, etc.) mapped from CSS custom properties in `globals.css`. Components must use these tokens rather than raw color values. The page-background utility is `bg-page` — the token is deliberately NOT named `--color-base` because that name collides with Tailwind's built-in `text-base` font-size key (see the Known Issues entry for the collision it caused).
- Sticky Navbar uses a translucent `bg-page/80` with backdrop blur; a future refinement may add scroll-state transparency behavior.
- Theme system uses next-themes (v0.4.6) with class-based dark mode (`attribute="class"`), `defaultTheme="system"`, and `enableSystem`. The `@custom-variant dark (&:is(.dark *))` in `globals.css` remains compatible.
- User-selected theme persists via next-themes localStorage. System preference is followed when no manual selection exists.
- The Navbar and Footer wordmark is a shared `Logo` component to keep the brand mark consistent across the shell.
- The project is in the mock-data development phase per the architecture Development Data Strategy: UI consumes data from `mock-database/` modules via server-side helpers in `lib/`; mock data definitions never live inside UI components. This layer will be replaced by Prisma/PostgreSQL queries later without rewriting UI.
- Social media links (Instagram, X, YouTube, Telegram) use **simple-icons brand icons** via the shared `components/layout/social-icons.tsx` (same exported component names; icons render the simple-icons brand paths). This supersedes the earlier stroke-style hand-drawn SVGs (Lucide 1.41.0 no longer ships brand icons).
- P4P rankings use a ranking-list presentation (not cards) per ui-context.md. Position numbers use Barlow Condensed font, ranking movement indicators use color-coded arrows (green for UP, red for DOWN, neutral dash for STABLE, accent for NEW). Rows are clickable links to fighter profiles.
- Fighters page uses a Server Component for the page shell and data fetching, with a Client Component (`FightersBrowser`) isolated for the interactive search/filter state. This follows the architecture principle of not converting entire pages to Client Components when only a portion requires interactivity.
- `lib/fighters.ts` exports `WEIGHT_CLASSES` as a const tuple to keep the canonical division list in one place; both the filter dropdown and any future weight-class logic reference this single source.
- Fighter card links use the existing `id` field (slugified names) as the URL slug, matching the convention already used by the P4P section and champions section links.
- Fighter Profile pages are fully static (SSG via `generateStaticParams`), so biographical age is computed at build time via `lib/calculate-age.ts`. Acceptable for the mock phase (static data); will need a runtime strategy when ages become dynamic.
- Fighter Profile uses inline `params` typing (`{ params: Promise<{ "fighter-slug": string }> }`) rather than relying on generated `.next/types` route types, keeping standalone `npx tsc --noEmit` valid. Next.js 16 makes `params` a Promise, awaited in the page, `generateMetadata`, and `generateStaticParams`.
- Contact channels (email, phone, Telegram) are static placeholder values defined locally in `components/contact/get-in-touch.tsx` for the mock-data phase: `contact@mmaorganization.com`, `+1 (555) 012-3456`, `https://t.me/MMAOrganization`. When production org contact details exist, they should replace these values (a single `ContactCard` item array) without any structural changes.

## Session Notes

- The project uses Next.js 16 (Turbopack), Tailwind CSS v4, shadcn/ui (base-nova style), Base UI React, and Lucide icons.
- `components/ui/button.tsx` (shadcn Base UI button) is a protected foundation component and was not modified.
- The existing shadcn button/theme tokens were remapped to the project token system (e.g. `--background` → `--bg-base`, `--foreground` → `--text-primary`) so base components and the new layout tokens stay consistent.
- `ThemeToggle` intentionally avoids calling `setState` inside an effect (lint rule `react-hooks/set-state-in-effect`); state is initialized to the default dark value.
- Social icon decision (superseded): Lucide 1.41.0 removed brand icons (verified no Instagram/Youtube/Telegram/Twitter exports). The project initially embedded inline stroke SVGs rather than add a dependency; per user direction this was later replaced — **simple-icons is now the social/brand icon source** (both the About Follow section and the Footer's shared `social-icons.tsx`), while Lucide remains for UI/functional icons.
- Nav and footer link targets (`/rankings`, `/fighters`, `/events`, `/news`, `/about`, `/contact`): all six routes now have page files and resolve — `/contact` is the last one to be implemented.
- The hero's related event data is placeholder mock data from `mock-database/events.ts`: upcoming "MMA — Night of Champions" (featured center card by default), a future "MMA — Warpath", plus four past/completed events; artwork URLs are external Unsplash images allowed via `next.config.ts` remote patterns. Replace with production data when the DB is connected.
- Fighter IDs in `mock-database/fighters.ts` use slugified names (e.g., `marcus-rivera`) instead of auto-generated IDs (e.g., `ftr-001`) for cleaner URL slugs in fighter profile links.
- The News carousel reads `prefers-reduced-motion` via `useSyncExternalStore` (subscribe to `matchMedia` change). The initial `useState`-in-effect approach was rejected because it trips the `react-hooks/set-state-in-effect` lint rule (same rule already noted for `ThemeToggle`); `useSyncExternalStore` is the idiomatic no-lint-violation alternative.
- News mock data (`mock-database/news.ts`) references Unsplash image URLs already whitelisted in `images.remotePatterns`; not all image IDs were verified to load at runtime, but a broken image degrades to the card's `bg-subtle` fallback without breaking the page.
- News article dates (June–September 2026) are consistent with the org's event timeline (MMA 37 Feb 2026 → Night of Champions Nov 2026, Warpath Feb 2027).
- `/news/[news-slug]` now resolves to the News Detail page (SSG, all 8 mock slugs) built per `context/features-developing/17-newsPageSlug.md`. The News Listing page was built first per 16-newsPage.md scope, which deferred the detail page to its own spec.
- Fighters page (`/fighters`) is now live and links resolve from the Navbar. Fighter card links point to `/fighters/[fighter-slug]` which will 404 until the Fighter Profile page is built.
- The `FightersBrowser` Client Component uses `useMemo` for filtered results to avoid re-filtering on every render when only unrelated state changes.
- The search input uses `sr-only` label for accessibility; the weight class filter uses a native `<select>` element for simplicity and accessibility rather than a custom dropdown component.
- Fighter card design follows the visual hierarchy specified in ui-context.md: image prominently displayed with dark gradient scrim, fighter name and nickname overlaid, weight class and record below, ranking metadata as secondary badges.
- Every fighter in `mock-database/fighters.ts` now carries `nationality`, `dateOfBirth` (`YYYY-MM-DD`, age computed dynamically per code-standards.md), `height`/`reach` (`F'IN / cm`), and `championships` (`MockFighterChampionship[]` of `{ title }`) for the upcoming Fighter Profile page. Champions list their reigning division title; select veterans list former titles. No fight data yet — that belongs to `mock-database/fights.ts` (not yet created).
- `MockFighter.imageUrl` is a required `string` set to `""` for all 128 fighters. UI components that render fighter images already handle a missing/empty URL via a `bg-subtle` fallback, so the placeholder paths (`/gaziev.jpg`, `/orolbai.jpg`) were safe to remove. Real image URLs will be added when the fighters page swaps to production data.
- The `text-ink` Tailwind utility fails to resolve correctly in certain component contexts — observed in champions card `<h3>` and fighter-profile `dd`/`h1`/`h2` elements, producing near-invisible text in both themes. The inline `style={{ color: "var(--text-primary)" }}` pattern is the established mitigation. The mechanism behind the `text-ink` resolution failure is not fully understood; further investigation may be warranted if the issue spreads to other components.
- Fighter Profile page is live: P4P section, Champions section, and Fighters browser links to `/fighters/[fighter-slug]` now resolve instead of 404-ing. Unknown slugs render Next's default 404 page (no custom `not-found.tsx` exists yet).
- The Fighter Profile hero's empty-`imageUrl` initials fallback follows the same fallback pattern already used across fighter cards.
- `mock-database/fights.ts` was created in February 2026, so Fighter Profile Phase 2 (Recent Fights + Fight History) and the Recent Results homepage carousel now have their data source.
- Note: `mock-database/news.ts` references a "Derek Lewis" heavyweight champion at MMA 40, but `mock-database/fighters.ts` lists shamil-gaziev as heavyweight champion — a pre-existing data inconsistency. `mock-database/fights.ts` books gaziev (not Lewis) in the MMA 40 superfight against jamal-washington to respect the existing fighter IDs.
- Fighter Profile Phase 2 (`lib/fights.ts` + `FighterFightRow` + Recent Fights/Fight History sections) keeps the Fighter Profile fully static (SSG): fight data is resolved server-side from the mock modules during prerender, same as Phase 1. `getFighterFights` filters out fights whose opponent/event references are unresolvable rather than weakening types or asserting — a documented data-integrity invariant already verified by the 11-matches data script.
- Result pill color coding on the fight rows follows the existing token conventions: WIN uses `accent-primary-dim`/`accent-primary` (accent = winner states per ui-context), LOSS uses neutral `bg-subtle`/`text-ink-secondary`, DRAW uses `warning`, NC uses muted, UPCOMING uses an outlined accent pill. Card positions are displayed on every row (including Preliminary), and fight dates come from each fight's own `YYYY-MM-DD` field formatted with `Intl.DateTimeFormat` (no third-party date library).
- All mock news slugs have a built News Detail page: `/news/[news-slug]` is SSG (8 pages) with `generateStaticParams`. The `/news/[news-slug]` links emitted by the home/listing News carousel and the News grid now resolve. The `/events/[event-slug]` links (from fight cards/fight rows) resolve to the Event Detail page.
- News mock data exposes a `category` field (`NewsCategory`: FIGHTERS / EVENTS / ORGANIZATION / RANKINGS) used by the News Listing page's category filter AND the News Detail Related News section (same-category-first ordering). This was the data-structure change first introduced for the News Listing task per 16-newsPage.md spec section 10; no news objects were duplicated in any component.
- Fighter Profile fight UI redesign: `FightMatchupCard` replaced `FighterFightRow` (file deleted). The card keeps `figure1`/`figure2` render order from `fights.ts` (never swapped), derives the selected fighter's result from `fight.result` via `lib/fights.ts` `outcome`, and shows the winner via `result.winnerId` (ring + "Winner" tag) which is independent of the selected fighter's perspective — center result reads from the profile fighter's perspective, winner tag marks the global winner. `FighterFight` gained `selfId`, `fighter1`, `fighter2`; `opponent` is retained as derived data for future consumers (e.g., the Recent Results homepage carousel).
- Event Detail page implements: the `FightMatchupCard` was extended (rather than duplicated) with `variant="event"` (neutral center result: global winner name, "Draw", or "No Contest"; footer shows bout weight class instead of a self-link) and `featured` (larger imagery/headline + accent ring for Main/Co-Main). `getEventFights` returns `FighterFight[]` (same shape as `getFighterFights`) preserving the `fights.ts` array order so group display order is the intended one. The page is fully SSG (the homepage `EventsCarousel`, fight-row/`FightMatchupCard` event links, and nav "Events" now resolve to either the Event Detail pages or the new `/events` listing page).
- `lib/fights.ts` now also exports `EventListItem` and `getEventListItems()`: it computes each event's `mainEvent` matchup (from `fights.ts` `MAIN_EVENT` per `eventId`, fighter names from `fighters.ts`) and `fightCount`, so the Events listing page derives main-event/number-of-fights information without duplicating fight data inside `events.ts` or UI components.
- Events Listing page structure: Page Header → Featured Events (timeline) → Search + Filters + All Events grid. The Featured timeline sits directly above the browser so search/filter controls stay adjacent to their results (a UX adaptation of the spec's enumerated structure, which the spec explicitly permits).
- The `/events` listing page is fully static (SSG, prerendered at build). Search and status filtering are client-side state inside `EventsBrowser` (matching the `FightersBrowser` pattern) rather than URL `searchParams`, so the page stays static and the mock-data layer is untouched.
- The All Events grid is sorted newest → oldest: `EventsBrowser` applies `.sort((a, b) => b.date.getTime() - a.date.getTime())` to the filtered results inside its `useMemo`, so search and status-filter results stay chronology-descending without touching the Featured Events timeline (which keeps the mock-database ascending date order). No data was duplicated or hardcoded.
- About page (`/about`) is live and the nav "About" link now resolves. It is fully static (SSG) and reuses established patterns only: the section-eyebrow + Barlow Condensed `h2` heading (with the inline `style={{ color: "var(--text-primary)" }}` workaround), the homepage-Hero gradient-overlay composition for the About hero, and the existing card language for the philosophy cards. The page has no client components, no API routes, no new dependencies beyond `simple-icons` for the social brand icons.
- About social icon decision (supersedes the earlier stroke-SVG note): the user directed that social/brand icons use simple-icons while Lucide continues to serve UI/functional icons. `simple-icons@^16.30.0` was added (it was not installed before). The About Follow section uses `siInstagram`, `siX`, `siYoutube`, `siTelegram`; the global `components/layout/social-icons.tsx` (hand-drawn stroke SVGs) remains untouched because the Footer still consumes it.
- About page "By the Numbers" derives: Divisions = `WEIGHT_CLASSES.length` (8), Events = `getEvents().length` (6), Fighters = `getAllFighters().length` (128), Completed Fights = `status === "COMPLETED"` count in `mock-database/fights.ts` (38). No statistics are hardcoded; the numbers update automatically if mock data changes.
- The About page uses two external Unsplash image URLs (About hero `photo-1517963879433-6ad2b056d712`, Who We Are `photo-1534438327276-14e5300c3a48`), both on the already-whitelisted `images.unsplash.com` host in `next.config.ts`.
- Contact page (`/contact`) is live and the nav "Contact" link now resolves — the last remaining nav/footer target. It is fully static (SSG, `/contact` prerendered) and mirrors the About page's patterns: the Contact hero clones the About hero composition (shorter `min-h-[45vh]/[55vh]`), the Follow Us section mirrors the About Follow the Organization section, the FAQ reuses the shadcn `Accordion` foundation (single client section, uncontrolled single-open), and the Get in Touch cards reuse the standard `bg-surface`/`border-line`/`rounded-lg` card language. No contact form, no map, no invented office address.
- Contact placeholder-data decision: the project defines no email/phone/Telegram for the organization, so the Contact page uses clearly-fictional brand-level placeholders in line with the mock-data phase (not personal-looking info): `contact@mmaorganization.com`, `+1 (555) 012-3456`, and `https://t.me/MMAOrganization`. They are static values in the `GetInTouch` component — no `mock-database/contact.ts`, no DB model, no API, per the 19-contactPage.md spec.
- Contact social icons reuse the shared `components/layout/social-icons.tsx` (simple-icons brand paths) for both the Get in Touch Telegram card and the Follow Us buttons — the same brand-icon source as the Footer, with the same URLs as the Footer and the About Follow section. No new icon library was installed (the About page already added `simple-icons@^16.30.0`).
